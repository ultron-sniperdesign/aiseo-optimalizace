---
import BaseLayout from "~/layouts/BaseLayout.astro";
import SniperDesignBigContact from "~/components/sniperdesign/BigContact.astro";
import * as p from "~/content/pages/sluzby";
import { buildFaqJsonLd } from "~/lib/jsonld";

/**
 * /sluzby/ — prodejní landing „Editorial důkaz" (koncept A, 2026-08).
 *
 * Vizuální jazyk: prémiový editorial — hairline linky, obří číslice,
 * „účetní výkaz" důkazů s tečkovanými vodicími linkami, stylizovaná GSC
 * karta v hero. Důkazy nesou přesvědčení, grafika ustupuje.
 *
 * Obsah: content/pages/sluzby.ts (per-mutace). Kotvy #kroky, #moduly a #obsah
 * jsou odkazované z detailních stránek retainerů — neměnit bez redirectu.
 * ProofStrip tu záměrně není: jeho čísla nese ledger, duplikace by je oslabila.
 */
const jsonLd = [buildFaqJsonLd(p.faq), p.offerCatalogJsonLd];

/** Glyf vlivu — mono, bez emoji (editorial tón): ● velký · ◐ střední · ○ menší */
const impactGlyph: Record<number, string> = { 3: "●", 2: "◐", 1: "○" };
const impactLabel: Record<number, string> = { 3: "velký vliv", 2: "střední vliv", 1: "menší vliv" };
const modulesByImpact = [...p.modules].sort((a, b) => b.impact - a.impact);
---

<BaseLayout
  title={p.meta.title}
  description={p.meta.description}
  image={p.meta.ogImage}
  jsonLd={jsonLd}
  breadcrumb={[{ label: "Služby", href: "/sluzby/" }]}
>
  <div class="ed">
    <!-- ===== HERO ===== -->
    <section class="hero" aria-labelledby="hero-title">
      <p class="hero__kicker r">{p.hero.kicker}</p>
      <h1 id="hero-title" class="hero__title r r2" set:html={p.hero.titleHtml}></h1>
      <div class="hero__grid">
        <div class="r r3">
          <p class="hero__lead" set:html={p.hero.lead}></p>
          <div class="hero__cta">
            <a class="btn btn--ink" href={p.hero.ctaPrimary.href}>{p.hero.ctaPrimary.label} <span aria-hidden="true">→</span></a>
            <a class="btn btn--ghost" href={p.hero.ctaSecondary.href}>{p.hero.ctaSecondary.label}</a>
          </div>
          <p class="hero__trust">{p.hero.trustLine}</p>
        </div>

        <figure class="shot r r4" aria-label={p.heroShot.ariaLabel}>
          <div class="shot__bar">
            <span class="shot__dot"></span><span class="shot__dot"></span><span class="shot__dot"></span>
            <span class="shot__title">{p.heroShot.barTitle}</span>
          </div>
          <div class="shot__body">
            <p class="shot__metric">{p.heroShot.metric} <small>{p.heroShot.metricUnit}</small></p>
            <p class="shot__sub">{p.heroShot.sub}</p>
            <div class="spark" aria-hidden="true">
              {p.heroShot.bars.map((h, i) => (
                <i style={`height:${h}%;animation-delay:${0.4 + i * 0.04}s`}></i>
              ))}
            </div>
          </div>
        </figure>
      </div>
    </section>

    <!-- ===== VÝKAZ DŮKAZŮ ===== -->
    <section class="ledger" aria-label="Výsledky z vlastních dat">
      <div class="ledger__head">
        <span>{p.ledger.headLeft}</span>
        <span>{p.ledger.headRight}</span>
      </div>
      {p.ledger.rows.map((row) => (
        <div class={`lrow${row.hi ? " lrow--hi" : ""}`}>
          <span class="lrow__label">{row.label}</span>
          <span class="lrow__leader" aria-hidden="true"></span>
          <span class="lrow__value">{row.value}<small>{row.unit}</small></span>
        </div>
      ))}
      <p class="ledger__src" set:html={p.ledger.srcHtml}></p>
    </section>

    <!-- ===== TŘI KROKY ===== -->
    <section class="steps" id="kroky" aria-labelledby="steps-title">
      <h2 id="steps-title" class="steps__intro" set:html={p.steps.introHtml}></h2>

      {p.steps.items.map((s, idx) => (
        <article class="step" id={idx === 1 ? "moduly" : undefined}>
          <div>
            <div class="step__num">{s.num}</div>
            <p class="step__meta">{s.meta}</p>
          </div>
          <div>
            <h3>{s.title}</h3>
            <p class="step__desc">{s.desc}</p>

            {idx === 1 && (
              <>
                <ul class="step__areas" role="list">
                  {modulesByImpact.map((m) => (
                    <li title={m.desc}>
                      <span class="step__glyph" aria-label={impactLabel[m.impact]}>{impactGlyph[m.impact]}</span>
                      {m.name}
                    </li>
                  ))}
                </ul>
                <p class="step__legend">{p.modulesFoot.impactLegend}</p>
                <p class="step__platform" set:html={p.modulesFoot.platformNoteHtml}></p>
              </>
            )}

            <p class="step__price" set:html={s.priceHtml}></p>
          </div>
        </article>
      ))}
    </section>

    <!-- ===== CENÍK OBSAHU ===== -->
    <section class="content" id="obsah" aria-labelledby="content-title">
      <div class="ledger__head">
        <span>{p.stepContent.eyebrow}</span>
        <span>ceny bez DPH</span>
      </div>
      <h2 id="content-title" class="content__title" set:html={p.stepContent.titleHtml}></h2>
      <p class="content__lead">{p.stepContent.lead}</p>

      {[p.categoryRows, p.productRows].map((group) => (
        <div class="ctable">
          <h3 class="ctable__title">{group.title}</h3>
          {group.rows.map((row) => (
            <div class={`crow${row.featured ? " crow--hi" : ""}`}>
              <div class="crow__main">
                <span class="crow__name">{row.name}</span>
                <span class="crow__detail">{row.detail}</span>
              </div>
              <span class="lrow__leader" aria-hidden="true"></span>
              <div class="crow__buy">
                <span class="crow__price">{row.price} <small>{row.unit}</small></span>
                <span class="crow__analysis">+ {row.analysis}</span>
              </div>
            </div>
          ))}
        </div>
      ))}

      <div class="crow crow--compare">
        <div class="crow__main">
          <span class="crow__name">{p.compareOffer.name}</span>
          <span class="crow__detail">{p.compareOffer.detail}</span>
        </div>
        <span class="lrow__leader" aria-hidden="true"></span>
        <div class="crow__buy">
          <span class="crow__price">{p.compareOffer.price} <small>{p.compareOffer.unit}</small></span>
        </div>
      </div>

      <aside class="analysis">
        <h3>{p.stepContent.analysisTitle}</h3>
        <p set:html={p.stepContent.analysisHtml}></p>
      </aside>
    </section>

    <!-- ===== AUDIT NABÍDKA ===== -->
    <section class="offer" id="audit" aria-labelledby="offer-title">
      <div>
        <h2 id="offer-title" class="offer__title" set:html={p.offer.titleHtml}></h2>
        <p class="offer__lead">{p.offer.lead}</p>
        <ul class="offer__list" role="list">
          {p.offer.items.map((i) => <li>{i}</li>)}
        </ul>
      </div>
      <div class="offer__buy">
        <p class="offer__price">{p.offer.price}</p>
        <p class="offer__vat">{p.offer.vat}</p>
        <a class="btn btn--ink" href={p.offer.cta.href}>{p.offer.cta.label} <span aria-hidden="true">→</span></a>
        <p class="offer__risk">{p.offer.risk}</p>
      </div>
    </section>

    <!-- ===== DLOUHODOBĚ ===== -->
    <section class="ret" aria-labelledby="ret-title">
      <div class="ledger__head">
        <span>{p.retainers.eyebrow}</span>
        <span>měsíční spolupráce</span>
      </div>
      <h2 id="ret-title" class="content__title" set:html={p.retainers.titleHtml}></h2>
      <p class="content__lead">{p.retainers.lead}</p>
      <div class="ret__grid">
        {p.retainers.items.map((r) => (
          <a class="ret__card" href={r.href}>
            <span class="ret__type">{r.type}</span>
            <h3>{r.name}</h3>
            <p>{r.desc}</p>
            <span class="ret__cta">Detail služby <span aria-hidden="true">→</span></span>
          </a>
        ))}
      </div>
      <p class="ret__measure">{p.retainers.measureNote}</p>
    </section>

    <!-- ===== SELF-CHECK ===== -->
    <section class="check-sec" aria-labelledby="check-title">
      <div class="ledger__head">
        <span>{p.selfCheck.eyebrow}</span>
        <span class="check__meter" data-check-progress>
          {p.selfCheck.progressTemplate.replace("{n}", "0").replace("{total}", String(p.selfCheck.items.length))}
        </span>
      </div>
      <h2 id="check-title" class="content__title" set:html={p.selfCheck.titleHtml}></h2>
      <p class="content__lead">{p.selfCheck.lead}</p>

      <div class="check" id="ai-selfcheck" data-progress-template={p.selfCheck.progressTemplate}>
        <ul class="check__list" role="list">
          {p.selfCheck.items.map((item) => (
            <li>
              <label class="check__label">
                <input type="checkbox" class="check__box" />
                <span>{item}</span>
              </label>
            </li>
          ))}
        </ul>
        <div class="check__foot">
          <p class="check__hint">{p.selfCheck.hint}</p>
          <a href={p.selfCheck.ctaHref} class="btn btn--ink">{p.selfCheck.cta} <span aria-hidden="true">→</span></a>
        </div>
      </div>
    </section>

    <!-- ===== MÝTY ===== -->
    <section class="myths-sec" aria-labelledby="myths-title">
      <div class="ledger__head">
        <span>{p.myths.eyebrow}</span>
        <span></span>
      </div>
      <h2 id="myths-title" class="content__title" set:html={p.myths.titleHtml}></h2>
      <p class="content__lead">{p.myths.lead}</p>
      <div class="myths">
        {p.myths.items.map((m) => (
          <article class="myth">
            <h3>{m.name}</h3>
            <p>{m.desc}</p>
          </article>
        ))}
      </div>
    </section>

    <!-- ===== FAQ ===== -->
    <section class="faq-sec" aria-labelledby="faq-title">
      <div class="ledger__head">
        <span>{p.faqHead.eyebrow}</span>
        <span></span>
      </div>
      <h2 id="faq-title" class="content__title" set:html={p.faqHead.titleHtml}></h2>
      <div class="faq">
        {p.faq.map((f, i) => (
          <details class="faq__item" open={i === 0}>
            <summary>{f.q}</summary>
            <p>{f.a}</p>
          </details>
        ))}
      </div>
    </section>

    <!-- ===== FINÁLNÍ CTA ===== -->
    <section class="final" aria-labelledby="final-title">
      <h2 id="final-title" class="final__title" set:html={p.finalCta.titleHtml}></h2>
      <p class="final__lead">{p.finalCta.lead}</p>
      <div class="final__btns">
        <a class="btn btn--ink" href={p.finalCta.btnPrimary.href}>{p.finalCta.btnPrimary.label} <span aria-hidden="true">→</span></a>
        <a class="btn btn--ghost" href={p.finalCta.btnSecondary.href}>{p.finalCta.btnSecondary.label}</a>
      </div>
    </section>

    <SniperDesignBigContact eyebrowNum="07" />
  </div>
</BaseLayout>

<script>
  /** Self-check: počítadlo zaškrtnutých položek (bez ukládání, čistě klientské). */
  const root = document.getElementById("ai-selfcheck");
  if (root) {
    const boxes = root.querySelectorAll<HTMLInputElement>(".check__box");
    const progress = document.querySelector("[data-check-progress]");
    const template = root.getAttribute("data-progress-template") ?? "{n} / {total}";
    const update = () => {
      const n = [...boxes].filter((b) => b.checked).length;
      if (progress)
        progress.textContent = template.replace("{n}", String(n)).replace("{total}", String(boxes.length));
    };
    boxes.forEach((b) => b.addEventListener("change", update));
  }
</script>

<style>
  /* ============ EDITORIAL ZÁKLAD ============ */
  .ed {
    max-width: 76rem;
    margin: 0 auto;
    padding: 0 var(--space-section-x);
    font-size: 1.03rem;
  }
  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    font-family: var(--font-sans);
    font-weight: 500;
    font-size: 0.98rem;
    text-decoration: none;
    border-radius: 999px;
    padding: 0.85rem 1.5rem;
    transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
  }
  .btn--ink { background: var(--color-ink); color: var(--color-paper); }
  .btn--ink:hover { transform: translateY(-2px); box-shadow: 0 14px 30px -14px oklch(0.2 0.012 260 / 0.55); }
  .btn--ghost { border: 1px solid var(--color-rule-strong); color: var(--color-ink); }
  .btn--ghost:hover { border-color: var(--color-ink); }

  @keyframes rise { from { opacity: 0; transform: translateY(18px); } to { opacity: 1; transform: none; } }
  .r { animation: rise 0.7s cubic-bezier(0.2, 0.7, 0.2, 1) both; }
  .r2 { animation-delay: 0.12s; }
  .r3 { animation-delay: 0.24s; }
  .r4 { animation-delay: 0.36s; }
  @media (prefers-reduced-motion: reduce) {
    .r { animation: none; }
    .spark i { animation: none !important; }
    .btn { transition: none; }
  }

  /* mono „hlavička výkazu" — sdílený vzor sekcí */
  .ledger__head {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    font-family: var(--font-mono);
    font-size: 0.72rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--color-ink-faint);
  }

  /* ============ HERO ============ */
  .hero { padding-top: clamp(2.5rem, 6vw, 5rem); }
  .hero__kicker {
    font-family: var(--font-mono);
    font-size: 0.72rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--color-ink-faint);
    display: flex;
    gap: 1rem;
    align-items: center;
    margin: 0;
  }
  .hero__kicker::after { content: ""; flex: 1; height: 1px; background: var(--color-rule); }
  .hero__title {
    font-family: var(--font-sans);
    font-weight: 250;
    font-size: clamp(2.3rem, 6vw, 4.9rem);
    line-height: 1.03;
    letter-spacing: -0.045em;
    margin: 1.5rem 0 0;
    max-width: 20ch;
  }
  .hero__title :global(strong) { font-weight: 600; }
  .hero__title :global(.u) { box-shadow: inset 0 -0.14em 0 oklch(0.52 0.18 250 / 0.28); }
  .hero__grid {
    display: grid;
    grid-template-columns: minmax(0, 1.25fr) minmax(0, 1fr);
    gap: clamp(2rem, 5vw, 5rem);
    align-items: end;
    margin-top: 2.25rem;
  }
  .hero__lead { font-size: clamp(1rem, 1.5vw, 1.18rem); color: var(--color-ink-muted); max-width: 36rem; margin: 0; }
  .hero__lead :global(strong) { color: var(--color-ink); font-weight: 600; }
  .hero__cta { display: flex; gap: 0.9rem; flex-wrap: wrap; margin-top: 1.75rem; align-items: center; }
  .hero__trust { font-family: var(--font-mono); font-size: 0.72rem; color: var(--color-ink-faint); margin: 1rem 0 0; }

  .shot {
    border: 1px solid var(--color-rule);
    border-radius: 14px;
    background: var(--color-paper);
    box-shadow: 0 30px 60px -30px oklch(0.3 0.05 260 / 0.35);
    overflow: hidden;
    margin: 0;
  }
  .shot__bar { display: flex; gap: 0.4rem; padding: 0.65rem 0.9rem; border-bottom: 1px solid var(--color-rule); align-items: center; }
  .shot__dot { width: 9px; height: 9px; border-radius: 50%; background: var(--color-rule-strong); opacity: 0.5; }
  .shot__title { font-family: var(--font-mono); font-size: 0.66rem; color: var(--color-ink-faint); margin-left: 0.35rem; }
  .shot__body { padding: 1.1rem 1.2rem 1.3rem; }
  .shot__metric { font-family: var(--font-mono); font-size: clamp(1.8rem, 3vw, 2.5rem); letter-spacing: -0.02em; margin: 0; }
  .shot__metric small { font-size: 0.62em; color: var(--color-ink-faint); }
  .shot__sub { font-size: 0.8rem; color: var(--color-ink-muted); margin: 0.15rem 0 0; }
  .spark { display: flex; align-items: flex-end; gap: 3px; height: 74px; margin-top: 1rem; }
  .spark i {
    flex: 1;
    background: linear-gradient(180deg, oklch(0.62 0.16 250 / 0.9), var(--color-accent));
    border-radius: 2px 2px 0 0;
    transform-origin: bottom;
    animation: grow 1s cubic-bezier(0.2, 0.7, 0.2, 1) both;
  }
  @keyframes grow { from { transform: scaleY(0); } to { transform: scaleY(1); } }

  /* ============ LEDGER ============ */
  .ledger { margin-top: clamp(4rem, 9vw, 7rem); border-top: 1px solid var(--color-ink); padding-top: 0.9rem; }
  .lrow { display: flex; align-items: baseline; gap: 1rem; padding: 1.35rem 0; border-bottom: 1px solid var(--color-rule); }
  .lrow__label { font-size: clamp(1rem, 1.6vw, 1.25rem); font-weight: 500; letter-spacing: -0.01em; flex-shrink: 0; max-width: 26rem; }
  .lrow__leader { flex: 1; border-bottom: 2px dotted var(--color-rule-strong); transform: translateY(-6px); min-width: 2rem; }
  .lrow__value { font-family: var(--font-mono); font-size: clamp(1.6rem, 3.4vw, 2.8rem); letter-spacing: -0.03em; white-space: nowrap; }
  .lrow__value small { font-size: 0.45em; color: var(--color-ink-faint); }
  .lrow--hi .lrow__value { color: var(--color-accent-strong); }
  .ledger__src { font-family: var(--font-mono); font-size: 0.72rem; color: var(--color-ink-faint); padding-top: 0.8rem; margin: 0; }
  .ledger__src :global(a) { color: inherit; }

  /* ============ KROKY ============ */
  .steps { margin-top: clamp(4.5rem, 10vw, 8rem); }
  .steps__intro {
    font-family: var(--font-sans);
    font-weight: 250;
    font-size: clamp(1.9rem, 4vw, 3.1rem);
    letter-spacing: -0.04em;
    line-height: 1.05;
    max-width: 22ch;
    margin: 0;
  }
  .steps__intro :global(strong) { font-weight: 600; }
  .step {
    display: grid;
    grid-template-columns: minmax(0, 0.5fr) minmax(0, 1fr);
    gap: clamp(1.5rem, 4vw, 4rem);
    padding: clamp(2rem, 4.5vw, 3.25rem) 0;
    border-top: 1px solid var(--color-rule);
    margin-top: 2.5rem;
  }
  .step:first-of-type { margin-top: 3rem; }
  .step__num { font-family: var(--font-mono); font-size: clamp(3.4rem, 8vw, 6.2rem); line-height: 0.9; color: oklch(0.9 0.01 260); letter-spacing: -0.05em; }
  .step__meta { font-family: var(--font-mono); font-size: 0.72rem; letter-spacing: 0.14em; text-transform: uppercase; color: var(--color-accent-strong); margin: 0.9rem 0 0; }
  .step h3 { font-family: var(--font-sans); font-weight: 600; font-size: clamp(1.25rem, 2vw, 1.6rem); letter-spacing: -0.02em; margin: 0 0 0.7rem; }
  .step__desc { color: var(--color-ink-muted); max-width: 38rem; margin: 0; }
  .step__areas {
    list-style: none;
    margin: 1.2rem 0 0;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 0.5rem 1.25rem;
  }
  .step__areas li { font-size: 0.92rem; color: var(--color-ink-muted); display: flex; gap: 0.6rem; align-items: baseline; cursor: default; }
  .step__glyph { font-family: var(--font-mono); font-size: 0.72rem; color: var(--color-accent); flex-shrink: 0; }
  .step__legend { font-family: var(--font-mono); font-size: 0.7rem; color: var(--color-ink-faint); margin: 1rem 0 0; }
  .step__platform { font-size: 0.9rem; color: var(--color-ink-muted); margin: 0.75rem 0 0; max-width: 38rem; }
  .step__platform :global(a) { color: var(--color-accent-strong); }
  .step__price { font-family: var(--font-mono); font-size: 0.8rem; margin: 1.1rem 0 0; color: var(--color-ink); }
  .step__price :global(b) { font-size: 1.05rem; font-weight: 600; }
  .step__price :global(i) { font-style: normal; color: var(--color-ink-faint); }

  /* ============ CENÍK OBSAHU ============ */
  .content, .ret, .check-sec, .myths-sec, .faq-sec { margin-top: clamp(4.5rem, 10vw, 7.5rem); border-top: 1px solid var(--color-ink); padding-top: 0.9rem; }
  .content__title {
    font-family: var(--font-sans);
    font-weight: 250;
    font-size: clamp(1.8rem, 3.8vw, 2.9rem);
    letter-spacing: -0.038em;
    line-height: 1.06;
    margin: 1.4rem 0 0;
    max-width: 24ch;
  }
  .content__title :global(strong) { font-weight: 600; }
  .content__lead { color: var(--color-ink-muted); max-width: 40rem; margin: 0.9rem 0 0; }
  .ctable { margin-top: 2.25rem; }
  .ctable__title { font-family: var(--font-mono); font-size: 0.78rem; letter-spacing: 0.13em; text-transform: uppercase; color: var(--color-ink); margin: 0 0 0.35rem; }
  .crow { display: flex; align-items: baseline; gap: 1rem; padding: 1.05rem 0; border-bottom: 1px solid var(--color-rule); }
  .crow__main { flex-shrink: 0; max-width: 30rem; display: flex; flex-direction: column; gap: 0.15rem; }
  .crow__name { font-weight: 600; font-size: 1rem; letter-spacing: -0.012em; }
  .crow--hi .crow__name::after { content: "★ nejlepší hodnota"; font-family: var(--font-mono); font-size: 0.62rem; letter-spacing: 0.1em; text-transform: uppercase; color: var(--color-accent-strong); margin-left: 0.6rem; }
  .crow__detail { font-size: 0.86rem; color: var(--color-ink-muted); }
  .crow__buy { text-align: right; flex-shrink: 0; }
  .crow__price { font-family: var(--font-mono); font-size: clamp(1.15rem, 2vw, 1.55rem); letter-spacing: -0.02em; white-space: nowrap; }
  .crow__price small { font-size: 0.58em; color: var(--color-ink-faint); }
  .crow__analysis { display: block; font-family: var(--font-mono); font-size: 0.66rem; color: var(--color-ink-faint); margin-top: 0.2rem; }
  .crow--compare { margin-top: 2.25rem; border-top: 1px solid var(--color-rule); }
  .analysis { margin-top: 2.25rem; border-left: 3px solid var(--color-accent); background: var(--color-paper-deep); border-radius: 0 10px 10px 0; padding: 1.2rem 1.5rem; max-width: 46rem; }
  .analysis h3 { font-size: 1rem; font-weight: 600; margin: 0 0 0.4rem; letter-spacing: -0.012em; }
  .analysis p { font-size: 0.92rem; color: var(--color-ink-muted); margin: 0; }
  .analysis p :global(strong) { color: var(--color-ink); }

  /* ============ AUDIT NABÍDKA ============ */
  .offer {
    margin-top: clamp(4.5rem, 10vw, 7.5rem);
    border: 1px solid var(--color-ink);
    border-radius: 18px;
    padding: clamp(1.75rem, 4.5vw, 3.25rem);
    display: grid;
    grid-template-columns: minmax(0, 1.35fr) minmax(0, 1fr);
    gap: clamp(1.5rem, 4vw, 4rem);
    position: relative;
    overflow: hidden;
    background: var(--color-paper);
  }
  .offer::before {
    content: "AUDIT";
    position: absolute;
    right: -0.06em;
    top: -0.28em;
    font-family: var(--font-mono);
    font-size: clamp(5rem, 14vw, 11rem);
    letter-spacing: -0.05em;
    color: oklch(0.955 0.008 250);
    z-index: 0;
    pointer-events: none;
  }
  .offer > * { position: relative; }
  .offer__title { font-family: var(--font-sans); font-weight: 250; font-size: clamp(1.8rem, 3.6vw, 2.8rem); letter-spacing: -0.035em; line-height: 1.08; margin: 0; }
  .offer__title :global(strong) { font-weight: 600; }
  .offer__lead { color: var(--color-ink-muted); margin: 0.9rem 0 0; max-width: 34rem; }
  .offer__list { list-style: none; margin: 1.3rem 0 0; padding: 0; }
  .offer__list li { padding: 0.5rem 0 0.5rem 1.7rem; position: relative; border-bottom: 1px dotted var(--color-rule); }
  .offer__list li::before { content: "✓"; position: absolute; left: 0; color: var(--color-geo); font-weight: 600; }
  .offer__buy { align-self: center; text-align: center; border-left: 1px solid var(--color-rule); padding-left: clamp(1.5rem, 4vw, 4rem); }
  .offer__price { font-family: var(--font-mono); font-size: clamp(2.5rem, 5vw, 3.5rem); letter-spacing: -0.03em; margin: 0; }
  .offer__vat { font-family: var(--font-mono); font-size: 0.7rem; color: var(--color-ink-faint); letter-spacing: 0.1em; text-transform: uppercase; margin: 0.3rem 0 1.3rem; }
  .offer__risk { font-size: 0.85rem; color: var(--color-ink-muted); margin: 1rem auto 0; max-width: 24ch; }

  /* ============ RETAINERY ============ */
  .ret__grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(290px, 1fr)); gap: 1rem; margin-top: 2rem; }
  .ret__card {
    border: 1px solid var(--color-rule);
    border-radius: 14px;
    padding: 1.5rem;
    text-decoration: none;
    color: inherit;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    transition: border-color 0.22s ease, transform 0.22s ease;
    background: var(--color-paper);
  }
  .ret__card:hover { border-color: var(--color-ink); transform: translateY(-3px); }
  .ret__type { font-family: var(--font-mono); font-size: 0.66rem; letter-spacing: 0.12em; text-transform: uppercase; color: var(--color-accent-strong); }
  .ret__card h3 { font-size: 1.1rem; font-weight: 600; letter-spacing: -0.015em; margin: 0; }
  .ret__card p { font-size: 0.9rem; color: var(--color-ink-muted); margin: 0; }
  .ret__cta { font-size: 0.9rem; font-weight: 500; color: var(--color-accent-strong); margin-top: 0.4rem; }
  .ret__measure { font-family: var(--font-mono); font-size: 0.72rem; color: var(--color-ink-faint); margin: 1.5rem 0 0; max-width: 52rem; }

  /* ============ SELF-CHECK ============ */
  .check__meter { text-transform: none; letter-spacing: 0.06em; }
  .check { margin-top: 1.9rem; }
  .check__list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 0.5rem 1.5rem;
  }
  .check__label { display: flex; gap: 0.7rem; align-items: flex-start; padding: 0.6rem 0; border-bottom: 1px dotted var(--color-rule); font-size: 0.92rem; color: var(--color-ink-muted); cursor: pointer; }
  .check__label:hover { color: var(--color-ink); }
  .check__box { accent-color: var(--color-accent); width: 1.05rem; height: 1.05rem; margin-top: 0.2rem; flex-shrink: 0; }
  .check__label:has(.check__box:checked) { color: var(--color-ink); }
  .check__label:has(.check__box:checked) span { text-decoration: line-through; text-decoration-color: var(--color-rule-strong); }
  .check__foot { display: flex; justify-content: space-between; align-items: center; gap: 1.5rem; flex-wrap: wrap; margin-top: 1.75rem; }
  .check__hint { font-family: var(--font-mono); font-size: 0.72rem; color: var(--color-ink-faint); margin: 0; }

  /* ============ MÝTY ============ */
  .myths { display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: clamp(1.25rem, 3vw, 2.5rem); margin-top: 2rem; }
  .myth { border-top: 2px solid var(--color-ink); padding-top: 1rem; }
  .myth h3 { font-size: 1.05rem; font-weight: 600; letter-spacing: -0.015em; margin: 0 0 0.45rem; }
  .myth p { font-size: 0.92rem; color: var(--color-ink-muted); margin: 0; }

  /* ============ FAQ ============ */
  .faq { margin-top: 1.9rem; }
  .faq__item { border-bottom: 1px solid var(--color-rule); }
  .faq__item summary {
    font-weight: 600;
    font-size: 1.02rem;
    letter-spacing: -0.012em;
    padding: 1.05rem 2rem 1.05rem 0;
    cursor: pointer;
    list-style: none;
    position: relative;
  }
  .faq__item summary::-webkit-details-marker { display: none; }
  .faq__item summary::after { content: "+"; position: absolute; right: 0.25rem; top: 50%; transform: translateY(-50%); font-family: var(--font-mono); color: var(--color-ink-faint); transition: transform 0.2s ease; }
  .faq__item[open] summary::after { transform: translateY(-50%) rotate(45deg); }
  .faq__item p { color: var(--color-ink-muted); margin: 0 0 1.2rem; max-width: 46rem; font-size: 0.95rem; }

  /* ============ FINÁLNÍ CTA ============ */
  .final { margin: clamp(4.5rem, 10vw, 7.5rem) 0 clamp(3rem, 6vw, 5rem); text-align: center; }
  .final__title { font-family: var(--font-sans); font-weight: 250; font-size: clamp(2rem, 4.6vw, 3.5rem); letter-spacing: -0.04em; line-height: 1.06; margin: 0; }
  .final__title :global(strong) { font-weight: 600; }
  .final__lead { color: var(--color-ink-muted); max-width: 34rem; margin: 1.1rem auto 1.9rem; }
  .final__btns { display: flex; gap: 0.9rem; justify-content: center; flex-wrap: wrap; }

  /* ============ RESPONSIVE ============ */
  @media (max-width: 840px) {
    .hero__grid, .step, .offer { grid-template-columns: 1fr; }
    .offer__buy { border-left: 0; border-top: 1px solid var(--color-rule); padding-left: 0; padding-top: 1.5rem; }
    .lrow__label, .crow__main { max-width: 58vw; }
    .crow__buy { text-align: right; }
  }
  @media (max-width: 560px) {
    .crow { flex-wrap: wrap; }
    .crow__main { max-width: 100%; }
    .lrow__leader { min-width: 1rem; }
    .crow .lrow__leader { display: none; }
    .crow__buy { text-align: left; }
  }
</style>
