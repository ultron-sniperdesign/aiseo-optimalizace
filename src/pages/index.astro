---
import BaseLayout from "~/layouts/BaseLayout.astro";
import SniperDesignBigContact from "~/components/sniperdesign/BigContact.astro";
import FreeStrip from "~/components/blocks/FreeStrip.astro";
import * as p from "~/content/pages/index";
import { site } from "~/i18n/site";
---

<BaseLayout title={p.meta.title} description={p.meta.description} image="/og/default.png">
  <!-- HERO -->
  <section class="hero">
    <span class="hero__intro">{p.hero.intro}</span>

    <h1 class="display hero__title" set:html={p.hero.title}></h1>

    <p class="lead hero__lead">{p.hero.lead}</p>

    <p class="hero__pills-hint">
      <span class="hero__pills-hint-arrow" aria-hidden="true">↓</span>
      {p.hero.pillsHint}
    </p>

    <div class="hero__pills" role="tablist" aria-label={p.ui.pillsAria}>
      {p.pillDefs.map((pill) => (
        <button
          type="button"
          class="pill pill--btn"
          data-d={pill.d}
          data-pill-trigger={pill.d}
          aria-controls={`pill-panel-${pill.d}`}
          aria-expanded={pill.d === "geo" ? "true" : "false"}
          role="tab"
        >
          <span>{pill.label}</span>
          <svg class="pill__icon" width="11" height="11" viewBox="0 0 11 11" fill="none" aria-hidden="true">
            <path d="M5.5 1V10M1 5.5H10" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" />
          </svg>
        </button>
      ))}
    </div>

    <!-- Pill detail panel — shared, swaps content based on active pill -->
    <div class="pill-panels" data-pill-panels>
      {p.pillDefs.map((pill) => (
        <section
          class="pill-panel"
          id={`pill-panel-${pill.d}`}
          data-d={pill.d}
          data-pill-panel={pill.d}
          role="tabpanel"
          hidden={pill.d !== "geo"}
        >
          <header class="pill-panel__head">
            <span class="pill-panel__abbr">{pill.badge}</span>
            <h2 class="pill-panel__title">{pill.fullName}</h2>
            <button type="button" class="pill-panel__close" data-pill-close aria-label={p.ui.pillPanelClose}>
              <svg width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
                <path d="M1 1L13 13M13 1L1 13" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" />
              </svg>
            </button>
          </header>
          <p class="pill-panel__one">{pill.oneLine}</p>
          <p class="pill-panel__quick">{pill.quick}</p>
          <a href={pill.href} class="pill-panel__link">
            {p.ui.pillPanelLink}
            <svg width="13" height="13" viewBox="0 0 14 14" fill="none" aria-hidden="true">
              <path d="M1 7H13M13 7L7 1M13 7L7 13" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" />
            </svg>
          </a>
        </section>
      ))}
    </div>
  </section>

  <!-- HERO TRIO — stejný dotaz, tři rozhraní. Reálné HTML mockupy místo dekorativního obrázku. -->
  <section class="trio" aria-labelledby="trio-title">
    <header class="section__head">
      <span class="eyebrow"><span class="eyebrow__num">{p.trioHead.eyebrowNum}</span> {p.trioHead.eyebrow}</span>
      <h2 id="trio-title" class="section__title" set:html={p.trioHead.title}></h2>
    </header>

    <div class="trio__grid">
      <!-- Cell 1: Klasický Google -->
      <div class="trio__cell" data-tone="google">
        <aside class="verdict" data-tone="google" role="note" aria-label={p.trio.google.cellAria}>
          <h3 class="verdict__type">{p.trioVerdicts[0].type}</h3>
          <span class="verdict__key">{p.trioVerdicts[0].key}</span>
          <p class="verdict__val">{p.trioVerdicts[0].val}</p>
        </aside>
        <article class="tpanel" data-tone="google" aria-label={p.trio.google.panelAria}>
          <header class="tpanel__chrome">
            <div class="tpanel__dots" aria-hidden="true"><span></span><span></span><span></span></div>
            <span class="tpanel__brand">
              <span class="g g--b">G</span><span class="g g--r">o</span><span class="g g--y">o</span><span class="g g--b">g</span><span class="g g--g">l</span><span class="g g--r">e</span>
            </span>
          </header>
          <div class="tpanel__bar">
            <span class="tpanel__bar-icon" aria-hidden="true">⌕</span>
            <span class="tpanel__bar-q">{p.trio.query}</span>
          </div>
          <div class="tpanel__body">
            {p.trio.google.results.map((r) => (
              <article class="result">
                <span class="result__url">{site.name} › {r.path}</span>
                <h3 class="result__title" set:html={r.title}></h3>
                <p class="result__desc" set:html={r.desc}></p>
              </article>
            ))}
          </div>
        </article>
      </div>

      <!-- Cell 2: Google s AI Overview -->
      <div class="trio__cell" data-tone="aio">
        <aside class="verdict" data-tone="aio" role="note" aria-label={p.trio.aio.cellAria}>
          <h3 class="verdict__type">{p.trioVerdicts[1].type}</h3>
          <span class="verdict__key">{p.trioVerdicts[1].key}</span>
          <p class="verdict__val">{p.trioVerdicts[1].val}</p>
        </aside>
        <article class="tpanel tpanel--featured" data-tone="aio" aria-label={p.trio.aio.panelAria}>
          <header class="tpanel__chrome">
            <div class="tpanel__dots" aria-hidden="true"><span></span><span></span><span></span></div>
            <span class="tpanel__brand">
              <span class="g g--b">G</span><span class="g g--r">o</span><span class="g g--y">o</span><span class="g g--b">g</span><span class="g g--g">l</span><span class="g g--r">e</span>
            </span>
          </header>
          <div class="tpanel__bar">
            <span class="tpanel__bar-icon" aria-hidden="true">⌕</span>
            <span class="tpanel__bar-q">{p.trio.query}</span>
          </div>
          <div class="tpanel__body">
            <section class="aio-card">
              <header class="aio-card__head">
                <span class="aio-card__sparkle" aria-hidden="true">✦</span>
                <span class="aio-card__label">{p.trio.aio.label}</span>
              </header>
              <p class="aio-card__line" set:html={p.trio.aio.line1}></p>
              <p class="aio-card__line" set:html={p.trio.aio.line2}></p>
              <div class="aio-card__sources">
                <span class="aio-card__src-label">{p.trio.aio.sourcesLabel}</span>
                <span class="aio-card__src">{site.name}</span>
                {p.trio.aio.sources.map((s) => (
                  <span class="aio-card__src">{s}</span>
                ))}
              </div>
            </section>
            <article class="result result--demoted">
              <span class="result__url">{site.name} › {p.trio.aio.demotedPath}</span>
              <h3 class="result__title" set:html={p.trio.aio.demotedTitle}></h3>
            </article>
          </div>
        </article>
      </div>

      <!-- Cell 3: ChatGPT -->
      <div class="trio__cell" data-tone="chat">
        <aside class="verdict" data-tone="chat" role="note" aria-label={p.trio.chat.cellAria}>
          <h3 class="verdict__type">{p.trioVerdicts[2].type}</h3>
          <span class="verdict__key">{p.trioVerdicts[2].key}</span>
          <p class="verdict__val">{p.trioVerdicts[2].val}</p>
        </aside>
        <article class="tpanel" data-tone="chat" aria-label={p.trio.chat.panelAria}>
          <header class="tpanel__chrome">
            <div class="tpanel__dots" aria-hidden="true"><span></span><span></span><span></span></div>
            <span class="tpanel__brand tpanel__brand--chat">
              <span class="chat-mark" aria-hidden="true">✺</span>
              {p.trio.chat.brand}
            </span>
          </header>
          <div class="tpanel__body tpanel__body--chat">
            <div class="bubble bubble--user">
              <span class="bubble__avatar" aria-hidden="true">🙂</span>
              <p class="bubble__text">{p.trio.query}</p>
            </div>
            <div class="bubble bubble--ai">
              <span class="bubble__avatar bubble__avatar--ai" aria-hidden="true">✺</span>
              <div class="bubble__body">
                <p class="bubble__text">{p.trio.chat.answerLead}</p>
                <ul class="bubble__list">
                  {p.trio.chat.bullets.map((b) => (
                    <li set:html={b}></li>
                  ))}
                </ul>
                <div class="bubble__sources">
                  <span class="bubble__src-pill">{site.name}</span>
                  {p.trio.chat.sources.map((s) => (
                    <span class="bubble__src-pill">{s}</span>
                  ))}
                </div>
              </div>
            </div>
          </div>
        </article>
      </div>
    </div>

    <aside class="trio__callout" role="note">
      <span class="trio__callout-mark" aria-hidden="true">!</span>
      <p class="trio__callout-text">
        <Fragment set:html={p.trioCallout.text} />
        <span class="trio__callout-cont">{p.trioCallout.cont}</span>
      </p>
    </aside>
  </section>

  <!-- STAT BAR — proč to musíte řešit (e-shop / web owner pain points) -->
  <section class="stat-bar" aria-label="Proč začít s AI SEO">
    <div class="stat-bar__inner">
      {p.statBar.map((s) => (
        <div class="stat">
          <div class="stat__num" set:html={s.num}></div>
          <p class="stat__label">{s.label}</p>
          <p class="stat__src">{s.src}</p>
        </div>
      ))}
    </div>
  </section>

  <!-- QUARTET — 4 disciplíny -->
  <section class="section">
    <header class="section__head">
      <span class="eyebrow"><span class="eyebrow__num">{p.quartetHead.eyebrowNum}</span> {p.quartetHead.eyebrow}</span>
      <h2 class="section__title" set:html={p.quartetHead.title}></h2>
      <p class="section__intro">{p.quartetHead.lead}</p>
    </header>

    <div class="quartet">
      {
        p.quartet.map((q) => (
          <a href={q.href} class="qcard" data-d={q.d}>
            <h3 class="qcard__abbr">{q.abbr}</h3>
            <p class="qcard__name">{q.name}</p>
            <p class="qcard__desc">{q.desc}</p>
            <ul class="qcard__features">
              {q.features.map((f) => (
                <li>{f}</li>
              ))}
            </ul>
            <span class="qcard__cta">
              {p.ui.quartetCta} <span aria-hidden="true">→</span>
            </span>
          </a>
        ))
      }
    </div>
  </section>

  <!-- ANATOMY (Phase 7 content tweak — místo hub-spoke ukazujeme ideální AI-friendly stránku) -->
  <section class="anatomy-section">
    <div class="anatomy-section__inner">
      <header class="anatomy-section__head">
        <span class="eyebrow"><span class="eyebrow__num">{p.anatomyHead.eyebrowNum}</span> {p.anatomyHead.eyebrow}</span>
        <h2 class="section__title" set:html={p.anatomyHead.title}></h2>
        <p class="section__intro">{p.anatomyHead.lead}</p>
      </header>

      <div class="anatomy-section__layout">
        <svg
          class="anatomy-svg"
          viewBox="0 0 480 600"
          xmlns="http://www.w3.org/2000/svg"
          role="img"
          aria-label={p.wireframe.aria}
        >
          <!-- Page outline -->
          <rect x="20" y="20" width="440" height="560" rx="14" fill="var(--color-paper)" stroke="var(--color-rule-strong)" stroke-width="1.2"></rect>

          <!-- Browser bar (decorative) -->
          <line x1="20" y1="60" x2="460" y2="60" stroke="var(--color-rule)" stroke-width="1"></line>
          <circle cx="38" cy="40" r="3.5" fill="var(--color-rule-strong)"></circle>
          <circle cx="50" cy="40" r="3.5" fill="var(--color-rule-strong)"></circle>
          <circle cx="62" cy="40" r="3.5" fill="var(--color-rule-strong)"></circle>
          <rect x="80" y="32" width="360" height="16" rx="8" fill="var(--color-paper-deep)"></rect>

          <!-- ❶ H1 jako otázka -->
          <rect x="40" y="86" width="380" height="30" rx="4" fill="var(--color-paper-deep)"></rect>
          <text x="48" y="106" font-family="Geist, sans-serif" font-size="14" font-weight="600" fill="var(--color-ink)">{p.wireframe.h1}</text>
          <g class="anatomy-mark">
            <circle cx="446" cy="101" r="14" fill="var(--color-accent)"></circle>
            <text x="446" y="106" font-family="Geist, sans-serif" font-size="13" font-weight="600" fill="var(--color-paper)" text-anchor="middle">1</text>
          </g>

          <!-- ❷ Answer block -->
          <rect x="40" y="132" width="380" height="80" rx="6" fill="oklch(0.96 0.022 250)" stroke="oklch(0.86 0.06 250)" stroke-width="1"></rect>
          <rect x="40" y="132" width="3" height="80" fill="var(--color-accent)"></rect>
          <text x="56" y="153" font-family="JetBrains Mono, monospace" font-size="9" font-weight="600" fill="var(--color-accent)" letter-spacing="1">{p.wireframe.answerLabel}</text>
          <rect x="56" y="162" width="340" height="8" rx="2" fill="var(--color-rule-strong)" opacity="0.55"></rect>
          <rect x="56" y="176" width="320" height="8" rx="2" fill="var(--color-rule-strong)" opacity="0.55"></rect>
          <rect x="56" y="190" width="280" height="8" rx="2" fill="var(--color-rule-strong)" opacity="0.55"></rect>
          <g class="anatomy-mark">
            <circle cx="446" cy="172" r="14" fill="var(--color-accent)"></circle>
            <text x="446" y="177" font-family="Geist, sans-serif" font-size="13" font-weight="600" fill="var(--color-paper)" text-anchor="middle">2</text>
          </g>

          <!-- ❸ Body text s fakty -->
          <rect x="40" y="232" width="380" height="6" rx="2" fill="var(--color-rule)"></rect>
          <rect x="40" y="244" width="360" height="6" rx="2" fill="var(--color-rule)"></rect>
          <rect x="40" y="256" width="380" height="6" rx="2" fill="var(--color-rule)"></rect>
          <rect x="40" y="268" width="200" height="6" rx="2" fill="var(--color-rule)"></rect>
          <!-- inline highlighted "fact" -->
          <rect x="246" y="266" width="60" height="10" rx="2" fill="var(--color-accent)" opacity="0.18"></rect>
          <g class="anatomy-mark">
            <circle cx="446" cy="253" r="14" fill="var(--color-accent)"></circle>
            <text x="446" y="258" font-family="Geist, sans-serif" font-size="13" font-weight="600" fill="var(--color-paper)" text-anchor="middle">3</text>
          </g>

          <!-- ❹ H2 sub-otázka -->
          <text x="40" y="316" font-family="Geist, sans-serif" font-size="13" font-weight="600" fill="var(--color-ink)">{p.wireframe.h2}</text>
          <rect x="40" y="328" width="380" height="6" rx="2" fill="var(--color-rule)"></rect>
          <rect x="40" y="340" width="340" height="6" rx="2" fill="var(--color-rule)"></rect>
          <g class="anatomy-mark">
            <circle cx="446" cy="316" r="14" fill="var(--color-accent)"></circle>
            <text x="446" y="321" font-family="Geist, sans-serif" font-size="13" font-weight="600" fill="var(--color-paper)" text-anchor="middle">4</text>
          </g>

          <!-- ❺ FAQ -->
          <rect x="40" y="370" width="380" height="120" rx="6" fill="var(--color-paper-deep)" stroke="var(--color-rule)" stroke-width="1"></rect>
          <text x="56" y="392" font-family="JetBrains Mono, monospace" font-size="9" font-weight="600" fill="var(--color-accent)" letter-spacing="1">{p.wireframe.faqLabel}</text>
          <text x="56" y="412" font-family="Geist, sans-serif" font-size="11" font-weight="600" fill="var(--color-ink)">{p.wireframe.faqQ1}</text>
          <rect x="56" y="420" width="340" height="5" rx="2" fill="var(--color-rule)"></rect>
          <rect x="56" y="430" width="320" height="5" rx="2" fill="var(--color-rule)"></rect>
          <text x="56" y="456" font-family="Geist, sans-serif" font-size="11" font-weight="600" fill="var(--color-ink)">{p.wireframe.faqQ2}</text>
          <rect x="56" y="464" width="340" height="5" rx="2" fill="var(--color-rule)"></rect>
          <rect x="56" y="474" width="280" height="5" rx="2" fill="var(--color-rule)"></rect>
          <g class="anatomy-mark">
            <circle cx="446" cy="430" r="14" fill="var(--color-accent)"></circle>
            <text x="446" y="435" font-family="Geist, sans-serif" font-size="13" font-weight="600" fill="var(--color-paper)" text-anchor="middle">5</text>
          </g>

          <!-- ❻ Schema markup (footer area) -->
          <rect x="40" y="510" width="380" height="48" rx="6" fill="var(--color-ink)"></rect>
          <text x="56" y="532" font-family="JetBrains Mono, monospace" font-size="10" fill="oklch(0.78 0.13 145)">&lt;script type="application/ld+json"&gt;</text>
          <text x="56" y="548" font-family="JetBrains Mono, monospace" font-size="10" fill="oklch(0.85 0.06 220)">"@type": "FAQPage", "mainEntity": [...]</text>
          <g class="anatomy-mark">
            <circle cx="446" cy="534" r="14" fill="var(--color-accent)"></circle>
            <text x="446" y="539" font-family="Geist, sans-serif" font-size="13" font-weight="600" fill="var(--color-paper)" text-anchor="middle">6</text>
          </g>
        </svg>

        <ol class="anatomy-list">
          {p.anatomyList.map((item, i) => (
            <li class="anatomy-item">
              <span class="anatomy-item__num" aria-hidden="true">{i + 1}</span>
              <div class="anatomy-item__body">
                <h3 class="anatomy-item__title">{item.title}</h3>
                <p class="anatomy-item__desc" set:html={item.desc}></p>
              </div>
            </li>
          ))}
        </ol>
      </div>
    </div>
  </section>

  <!-- FREE PDF PROMO STRIP — bridge mezi „Anatomií" (technický detail) a „Hlavním průvodcem" (teoretická hloubka).
       Logika: čtenář právě viděl 6 signálů AI-friendly stránky, přirozeně myslí „jak to dostat na moji homepage?" —
       PDF zdarma je hotová šablona přesně pro tento moment. -->
  {p.freeStrip && (
    <FreeStrip
      title={p.freeStrip.title}
      titleHighlight={p.freeStrip.titleHighlight}
    />
  )}

  <!-- PILLAR PROMO -->
  <section class="pillar-promo">
    <div class="pillar-promo__main">
      <span class="eyebrow"><span class="eyebrow__num">{p.pillarPromo.eyebrowNum}</span> {p.pillarPromo.eyebrow}</span>
      <h2 class="pillar-promo__title" set:html={p.pillarPromo.title}></h2>
      <p class="pillar-promo__lead">{p.pillarPromo.lead}</p>
      <div class="pillar-promo__meta">
        {p.pillarPromo.meta.map((m) => (
          <span>{m}</span>
        ))}
      </div>
      <a href={p.pillarPromo.btnHref} class="btn">
        {p.pillarPromo.btnLabel}
        <svg class="btn__arrow" width="14" height="14" viewBox="0 0 14 14" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <path d="M1 7H13M13 7L7 1M13 7L7 13" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"></path>
        </svg>
      </a>
    </div>

    <div class="pillar-promo__excerpt">
      <span class="pillar-promo__excerpt-mark">{p.pillarPromo.excerptMark}</span>
      <p class="pillar-promo__excerpt-text" set:html={p.pillarPromo.excerptText}></p>
    </div>
  </section>

  <!-- VYBRANÉ ČLÁNKY (fork skryje prázdným polem scards) -->
  {p.scards.length > 0 && (
  <section class="section section--deep">
    <header class="section__head">
      <span class="eyebrow"><span class="eyebrow__num">{p.articlesHead.eyebrowNum}</span> {p.articlesHead.eyebrow}</span>
      <h2 class="section__title" set:html={p.articlesHead.title}></h2>
    </header>

    <div class="section-grid">
      {p.scards.map((card) => (
        <a href={card.href} class="scard" data-d={card.d}>
          <div class="scard__header">
            <span class="scard__tag">{card.tag}</span>
            <span class="scard__time">{card.time}</span>
          </div>
          <h3 class="scard__title">{card.title}</h3>
          <p class="scard__desc">{card.desc}</p>
          <div class="scard__meta">
            <span>{card.words}</span>
            <span class="scard__cta">{p.ui.scardCta} <span aria-hidden="true">→</span></span>
          </div>
        </a>
      ))}
    </div>

    <div class="section-more">
      <a href={p.articlesMore.href} class="section-more__btn">
        {p.articlesMore.label}
        <svg width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
          <path d="M1 7H13M13 7L7 1M13 7L7 13" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </a>
    </div>
  </section>
  )}

  <!-- NABÍDKA — 3 produkty (Free / Pack / Audit) — fork skryje prázdným polem offers -->
  {p.offers.length > 0 && (
  <section class="offers" aria-labelledby="offers-title">
    <header class="section__head">
      <span class="eyebrow"><span class="eyebrow__num">{p.offersHead.eyebrowNum}</span> {p.offersHead.eyebrow}</span>
      <h2 id="offers-title" class="section__title" set:html={p.offersHead.title}></h2>
      <p class="section__intro" set:html={p.offersHead.lead}></p>
    </header>

    <div class="offers__grid">
      {p.offers.map((o) => (
        <a href={o.href} class={`offer offer--${o.mod}`}>
          {o.featuredPill && <span class="offer__featured-pill">{o.featuredPill}</span>}
          <header class="offer__head">
            <span class="offer__tag" set:html={o.tag}></span>
            <span class={`offer__price offer__price--${o.mod}`}>
              <strong set:html={o.priceMain}></strong>
              <small set:html={o.priceNote}></small>
              {o.priceVat && <small class="offer__price-vat" set:html={o.priceVat}></small>}
            </span>
          </header>
          <h3 class="offer__title" set:html={o.title}></h3>
          <p class="offer__desc" set:html={o.desc}></p>
          <ul class="offer__bullets">
            {o.bullets.map((b) => (
              <li set:html={b}></li>
            ))}
          </ul>
          <span class="offer__cta">
            {o.cta}
            <svg width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
              <path d="M1 7H13M13 7L7 1M13 7L7 13" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"></path>
            </svg>
          </span>
        </a>
      ))}
    </div>
  </section>
  )}

  <SniperDesignBigContact eyebrowNum={p.bigContactEyebrowNum} />
</BaseLayout>

<style>
  /* ===== TYPOGRAPHY ===== */
  .display {
    font-family: var(--font-sans);
    font-weight: 250;
    font-size: clamp(2.25rem, 6.5vw, 6rem);
    line-height: 0.95;
    letter-spacing: -0.045em;
  }
  .display :global(strong) { font-weight: 600; color: var(--color-ink); }
  .display :global(.hl) { color: var(--color-accent); font-weight: 400; }

  .lead {
    font-family: var(--font-sans);
    font-weight: 350;
    font-size: clamp(1.0625rem, 1.6vw, 1.3125rem);
    line-height: 1.5;
    color: var(--color-ink-muted);
    letter-spacing: -0.005em;
  }

  /* ===== HERO ===== */
  .hero {
    max-width: var(--max-section);
    margin: 0 auto;
    padding: clamp(3.5rem, 11vh, 8rem) var(--space-section-x) clamp(3.5rem, 7vh, 6rem);
  }
  .hero__intro {
    margin-bottom: 3rem;
    display: inline-flex;
    align-items: center;
    gap: 0.625rem;
    font-family: var(--font-sans);
    font-size: 0.875rem;
    color: var(--color-ink-muted);
    padding: 0.45rem 0.875rem;
    background: var(--color-paper-deep);
    border: 1px solid var(--color-rule);
    border-radius: 999px;
  }
  .hero__intro::before {
    content: "";
    display: inline-block;
    width: 7px;
    height: 7px;
    background: var(--color-accent);
    border-radius: 50%;
  }
  .hero__title {
    margin-bottom: 2.5rem;
    max-width: 19ch;
  }
  .hero__lead {
    max-width: 38rem;
    margin-bottom: 3rem;
  }
  .hero__pills-hint {
    font-family: var(--font-sans);
    font-size: 0.8125rem;
    color: var(--color-ink-muted);
    margin: 0 0 0.625rem;
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
  }
  .hero__pills-hint-arrow {
    font-size: 0.875rem;
    color: var(--color-geo);
    animation: hintBob 1.6s ease-in-out infinite;
  }
  @keyframes hintBob {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(3px); }
  }
  .hero__pills {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  /* hero stagger animation */
  @keyframes rise {
    from {
      opacity: 0;
      transform: translateY(12px);
    }
    to {
      opacity: 1;
      transform: none;
    }
  }
  .hero > * {
    animation: rise 0.7s cubic-bezier(0.2, 0.7, 0.2, 1) backwards;
  }
  .hero > *:nth-child(1) { animation-delay: 0.04s; }
  .hero > *:nth-child(2) { animation-delay: 0.14s; }
  .hero > *:nth-child(3) { animation-delay: 0.26s; }
  .hero > *:nth-child(4) { animation-delay: 0.34s; }
  .hero > *:nth-child(5) { animation-delay: 0.42s; }
  .hero > *:nth-child(6) { animation-delay: 0.5s; }

  .pill {
    font-family: var(--font-sans);
    font-size: 0.875rem;
    font-weight: 500;
    letter-spacing: -0.005em;
    padding: 0.45rem 0.875rem;
    border-radius: 999px;
    border: 1px solid var(--color-rule-strong);
    color: var(--color-ink-muted);
    background: var(--color-paper-deep);
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
  }
  .pill::before {
    content: "";
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--color-ink-faint);
  }
  .pill[data-d="seo"] {
    color: var(--color-seo);
    border-color: var(--color-seo-soft);
    background: var(--color-seo-tint);
  }
  .pill[data-d="seo"]::before { background: var(--color-seo); }
  .pill[data-d="geo"] {
    color: var(--color-geo);
    border-color: var(--color-geo-soft);
    background: var(--color-geo-tint);
  }
  .pill[data-d="geo"]::before { background: var(--color-geo); }
  .pill[data-d="aeo"] {
    color: var(--color-aeo);
    border-color: var(--color-aeo-soft);
    background: var(--color-aeo-tint);
  }
  .pill[data-d="aeo"]::before { background: var(--color-aeo); }
  .pill[data-d="aio"] {
    color: var(--color-aio);
    border-color: var(--color-aio-soft);
    background: var(--color-aio-tint);
  }
  .pill[data-d="aio"]::before { background: var(--color-aio); }

  /* Interactive pill (button) */
  .pill--btn {
    cursor: pointer;
    font: inherit;
    font-family: var(--font-sans);
    font-size: 0.875rem;
    font-weight: 500;
    transition: transform 0.15s ease, box-shadow 0.15s ease, border-color 0.15s ease;
    position: relative;
    box-shadow: 0 1px 2px -1px rgba(15, 23, 42, 0.08), 0 1px 0 rgba(15, 23, 42, 0.04);
  }
  .pill--btn:hover {
    transform: translateY(-1px);
    box-shadow: 0 4px 12px -4px rgba(15, 23, 42, 0.18);
    border-color: currentColor;
  }
  .pill--btn:focus-visible {
    outline: 2px solid currentColor;
    outline-offset: 2px;
  }
  .pill__icon {
    opacity: 0.55;
    transition: transform 0.2s ease, opacity 0.2s ease;
  }
  .pill--btn:hover .pill__icon { opacity: 1; }
  .pill--btn[aria-expanded="true"] .pill__icon {
    transform: rotate(45deg);
    opacity: 1;
  }
  .pill--btn[aria-expanded="true"] {
    box-shadow: 0 0 0 2px currentColor inset;
  }

  /* Pill detail panels — shared container, one visible at a time */
  .pill-panels {
    margin-top: 1rem;
    max-width: 42rem;
  }
  .pill-panel {
    border-radius: 12px;
    padding: 1.25rem 1.5rem 1.5rem;
    background: var(--color-paper);
    border: 1px solid var(--color-rule-strong);
    animation: pillPanelIn 0.22s cubic-bezier(0.2, 0.7, 0.2, 1);
  }
  .pill-panel[hidden] { display: none; }
  @keyframes pillPanelIn {
    from { opacity: 0; transform: translateY(-4px); }
    to { opacity: 1; transform: none; }
  }
  .pill-panel[data-d="seo"] { border-color: var(--color-seo-soft); background: color-mix(in oklch, var(--color-seo-tint) 60%, var(--color-paper)); }
  .pill-panel[data-d="geo"] { border-color: var(--color-geo-soft); background: color-mix(in oklch, var(--color-geo-tint) 60%, var(--color-paper)); }
  .pill-panel[data-d="aeo"] { border-color: var(--color-aeo-soft); background: color-mix(in oklch, var(--color-aeo-tint) 60%, var(--color-paper)); }
  .pill-panel[data-d="aio"] { border-color: var(--color-aio-soft); background: color-mix(in oklch, var(--color-aio-tint) 60%, var(--color-paper)); }

  .pill-panel__head {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 0.5rem;
  }
  .pill-panel__abbr {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.06em;
    padding: 0.2rem 0.5rem;
    border-radius: 4px;
    background: var(--color-paper);
    border: 1px solid currentColor;
  }
  .pill-panel[data-d="seo"] .pill-panel__abbr { color: var(--color-seo); }
  .pill-panel[data-d="geo"] .pill-panel__abbr { color: var(--color-geo); }
  .pill-panel[data-d="aeo"] .pill-panel__abbr { color: var(--color-aeo); }
  .pill-panel[data-d="aio"] .pill-panel__abbr { color: var(--color-aio); }

  .pill-panel__title {
    font-family: var(--font-sans);
    font-size: 1rem;
    font-weight: 600;
    letter-spacing: -0.01em;
    color: var(--color-ink);
    margin: 0;
    flex: 1;
  }
  .pill-panel__close {
    background: none;
    border: none;
    cursor: pointer;
    color: var(--color-ink-muted);
    padding: 0.25rem;
    display: inline-flex;
    border-radius: 6px;
    transition: background 0.15s ease, color 0.15s ease;
  }
  .pill-panel__close:hover {
    background: var(--color-paper-deep);
    color: var(--color-ink);
  }
  .pill-panel__one {
    font-family: var(--font-sans);
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--color-ink);
    margin: 0 0 0.5rem;
  }
  .pill-panel__quick {
    font-family: var(--font-sans);
    font-size: 0.9rem;
    line-height: 1.55;
    color: var(--color-ink-muted);
    margin: 0 0 1rem;
  }
  .pill-panel__link {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-family: var(--font-sans);
    font-size: 0.85rem;
    font-weight: 500;
    color: currentColor;
    text-decoration: none;
    border-bottom: 1px solid currentColor;
    padding-bottom: 1px;
    transition: gap 0.18s ease;
  }
  .pill-panel[data-d="seo"] .pill-panel__link { color: var(--color-seo); }
  .pill-panel[data-d="geo"] .pill-panel__link { color: var(--color-geo); }
  .pill-panel[data-d="aeo"] .pill-panel__link { color: var(--color-aeo); }
  .pill-panel[data-d="aio"] .pill-panel__link { color: var(--color-aio); }
  .pill-panel__link:hover { gap: 0.6rem; }

  /* ===== TRIO — 3 panely se stejným dotazem ===== */
  .trio {
    max-width: var(--max-section);
    margin: 0 auto;
    padding: var(--space-section-y) var(--space-section-x);
  }

  .trio__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.25rem;
    align-items: start;
  }
  @media (min-width: 720px) {
    .trio__grid { grid-template-columns: repeat(2, 1fr); }
  }
  @media (min-width: 1100px) {
    .trio__grid { grid-template-columns: repeat(3, 1fr); }
  }

  /* ── Trio cell — wrapper pro verdict bubble + tpanel ── */
  .trio__cell {
    display: flex;
    flex-direction: column;
    gap: 0.875rem; /* prostor mezi bubble a kartou — tail visí v tomto gapu */
  }
  /* Tone tokens — verdict + tail dědí stejnou paletu jako tpanel border. */
  .trio__cell[data-tone="google"] {
    --tone-accent: oklch(0.5 0.18 256);
    --tone-tint: oklch(0.97 0.025 256);
    --tone-soft: oklch(0.86 0.06 256);
  }
  .trio__cell[data-tone="aio"] {
    --tone-accent: var(--color-accent);
    --tone-tint: oklch(0.96 0.022 250);
    --tone-soft: oklch(0.86 0.06 250);
  }
  .trio__cell[data-tone="chat"] {
    --tone-accent: oklch(0.5 0.13 155);
    --tone-tint: oklch(0.96 0.022 155);
    --tone-soft: oklch(0.86 0.06 155);
  }

  /* ── Verdict bubble — vizuálně oddělená nad kartou, ale s tailem
   *    míří dolů ke kartě. Říká uživateli "co z toho rozhraní vyleze".
   *    Decentní tone-color pozadí, tučný text, monospace eyebrow.
   * ── */
  .verdict {
    position: relative;
    margin: 0;
    padding: 0.875rem 1.125rem 1rem;
    background: var(--tone-tint);
    border: 1px solid var(--tone-soft);
    border-left: 3px solid var(--tone-accent);
    border-radius: 12px;
    box-shadow: 0 2px 8px -4px color-mix(in oklch, var(--tone-accent) 25%, transparent);
  }
  /* Speech bubble tail — diamond rotated 45°, hangs in cell gap toward card. */
  .verdict::after {
    content: "";
    position: absolute;
    left: 1.5rem;
    bottom: -7px;
    width: 12px;
    height: 12px;
    background: var(--tone-tint);
    border-right: 1px solid var(--tone-soft);
    border-bottom: 1px solid var(--tone-soft);
    transform: rotate(45deg);
  }
  /* Type heading — identifikuje typ rozhraní (SERP / AIO / chatbot).
   * Hlavní vizuální kotva bubliny — uživatel hned ví, o jakém příkladu
   * mluvíme. Tone-accent barva, tučné, mírně větší než eyebrow. */
  .verdict__type {
    display: block;
    font-family: var(--font-sans);
    font-size: 0.95rem;
    font-weight: 600;
    line-height: 1.3;
    letter-spacing: -0.01em;
    color: var(--tone-accent);
    margin: 0 0 0.5rem;
    padding: 0 0 0.5rem;
    border-bottom: 1px solid color-mix(in oklch, var(--tone-accent) 18%, transparent);
  }
  .verdict__key {
    display: block;
    font-family: var(--font-mono);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-ink-muted);
    margin-bottom: 0.3rem;
  }
  .verdict__val {
    font-family: var(--font-sans);
    font-size: 1rem;
    font-weight: 600;
    color: var(--color-ink);
    line-height: 1.4;
    letter-spacing: -0.005em;
    margin: 0;
  }

  /* ── Panel base ── */
  .tpanel {
    background: var(--color-paper);
    border: 1px solid var(--color-rule-strong);
    border-radius: 12px;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    box-shadow: 0 1px 0 oklch(0 0 0 / 0.02), 0 8px 24px -16px oklch(0 0 0 / 0.18);
    transition: transform 0.25s ease, box-shadow 0.25s ease;
  }
  .tpanel:hover {
    transform: translateY(-2px);
    box-shadow: 0 1px 0 oklch(0 0 0 / 0.02), 0 12px 32px -16px oklch(0 0 0 / 0.22);
  }
  .tpanel--featured {
    border-color: color-mix(in oklch, var(--color-accent) 35%, var(--color-rule-strong));
  }

  /* Chrome (top bar with dots + brand) */
  .tpanel__chrome {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.55rem 0.875rem;
    background: var(--color-paper-deep);
    border-bottom: 1px solid var(--color-rule);
  }
  .tpanel__dots {
    display: inline-flex;
    gap: 0.3rem;
  }
  .tpanel__dots span {
    width: 9px;
    height: 9px;
    border-radius: 50%;
    background: var(--color-rule-strong);
  }
  .tpanel__brand {
    font-family: var(--font-sans);
    font-size: 0.95rem;
    font-weight: 600;
    letter-spacing: -0.01em;
    color: var(--color-ink);
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
  }
  .tpanel__brand--chat {
    color: oklch(0.42 0.13 155); /* deep emerald */
  }
  .chat-mark {
    color: oklch(0.42 0.13 155);
    font-size: 1.1em;
  }
  /* Google "Google" letter colors */
  .g--b { color: oklch(0.55 0.16 256); }
  .g--r { color: oklch(0.58 0.2 27); }
  .g--y { color: oklch(0.78 0.16 78); }
  .g--g { color: oklch(0.6 0.16 145); }

  /* Search bar */
  .tpanel__bar {
    display: flex;
    align-items: center;
    gap: 0.625rem;
    padding: 0.75rem 0.875rem;
    margin: 0.875rem;
    background: var(--color-paper);
    border: 1px solid var(--color-rule-strong);
    border-radius: 999px;
    font-family: var(--font-sans);
    font-size: 0.85rem;
    color: var(--color-ink);
  }
  .tpanel__bar-icon {
    color: var(--color-ink-muted);
    font-size: 1rem;
    line-height: 1;
  }
  .tpanel__bar-q {
    font-weight: 500;
    flex: 1;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  /* Body shared */
  .tpanel__body {
    padding: 0.5rem 0.875rem 0.875rem;
    display: flex;
    flex-direction: column;
    gap: 0.875rem;
    flex: 1;
  }

  /* ── Classic Google result ── */
  .result {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
  }
  .result__url {
    font-family: var(--font-sans);
    font-size: 0.72rem;
    color: oklch(0.34 0.01 260); /* AA pass na paper-deep (4.5+) */
  }
  .result__title {
    font-family: var(--font-sans);
    font-size: 0.95rem;
    font-weight: 600;
    line-height: 1.25;
    color: oklch(0.36 0.18 264); /* tmavší link blue, AA pass */
    margin: 0;
  }
  .result__desc {
    font-family: var(--font-sans);
    font-size: 0.78rem;
    line-height: 1.45;
    color: var(--color-ink-muted);
    margin: 0.15rem 0 0;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .result--demoted {
    opacity: 0.7;
  }

  /* ── AI Overview card ── */
  .aio-card {
    background: linear-gradient(
      135deg,
      color-mix(in oklch, var(--color-accent) 8%, var(--color-paper)) 0%,
      color-mix(in oklch, var(--color-aio) 6%, var(--color-paper)) 100%
    );
    border: 1px solid color-mix(in oklch, var(--color-accent) 25%, var(--color-rule-strong));
    border-radius: 10px;
    padding: 0.875rem 1rem;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }
  .aio-card__head {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    margin-bottom: 0.15rem;
  }
  .aio-card__sparkle {
    color: var(--color-accent);
    font-size: 0.85rem;
  }
  .aio-card__label {
    font-family: var(--font-sans);
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.04em;
    color: var(--color-accent);
    text-transform: uppercase;
  }
  .aio-card__line {
    font-family: var(--font-sans);
    font-size: 0.85rem;
    line-height: 1.5;
    color: var(--color-ink);
    margin: 0;
  }
  .aio-card__line :global(strong) {
    font-weight: 600;
  }
  .aio-card__sources {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
    align-items: center;
    padding-top: 0.4rem;
    border-top: 1px dashed color-mix(in oklch, var(--color-accent) 20%, var(--color-rule));
    margin-top: 0.25rem;
  }
  .aio-card__src-label {
    font-family: var(--font-sans);
    font-size: 0.7rem;
    color: var(--color-ink-muted);
    margin-right: 0.15rem;
  }
  .aio-card__src {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    padding: 0.2rem 0.5rem;
    background: var(--color-paper);
    border: 1px solid color-mix(in oklch, var(--color-accent) 25%, var(--color-rule));
    border-radius: 999px;
    color: var(--color-ink);
  }

  /* ── ChatGPT bubbles ── */
  .tpanel__body--chat {
    background: var(--color-paper-deep);
    padding: 0.875rem;
    gap: 0.625rem;
  }
  .bubble {
    display: flex;
    gap: 0.5rem;
    align-items: flex-start;
  }
  .bubble--user {
    justify-content: flex-end;
  }
  .bubble--user .bubble__text {
    background: var(--color-paper);
    border: 1px solid var(--color-rule-strong);
    color: var(--color-ink);
  }
  .bubble--ai .bubble__body {
    background: var(--color-paper);
    border: 1px solid color-mix(in oklch, var(--color-geo) 18%, var(--color-rule-strong));
    border-radius: 10px;
    padding: 0.625rem 0.75rem;
    flex: 1;
  }
  .bubble__avatar {
    width: 22px;
    height: 22px;
    flex-shrink: 0;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: var(--color-paper);
    border: 1px solid var(--color-rule);
    border-radius: 50%;
    font-size: 0.7rem;
  }
  .bubble__avatar--ai {
    background: color-mix(in oklch, var(--color-geo) 14%, var(--color-paper));
    border-color: color-mix(in oklch, var(--color-geo) 30%, transparent);
    color: oklch(0.42 0.13 155);
  }
  .bubble--user .bubble__avatar { order: 2; }
  .bubble__text {
    font-family: var(--font-sans);
    font-size: 0.825rem;
    line-height: 1.45;
    margin: 0;
    padding: 0.5rem 0.75rem;
    border-radius: 10px;
    max-width: 85%;
  }
  .bubble--ai .bubble__text {
    padding: 0;
    color: var(--color-ink);
    margin-bottom: 0.4rem;
  }
  .bubble__list {
    margin: 0 0 0.5rem;
    padding: 0 0 0 1rem;
    font-family: var(--font-sans);
    font-size: 0.8rem;
    line-height: 1.5;
    color: var(--color-ink);
  }
  .bubble__list li { margin-bottom: 0.25rem; }
  .bubble__list :global(strong) { font-weight: 600; }
  .bubble__sources {
    display: flex;
    flex-wrap: wrap;
    gap: 0.35rem;
    padding-top: 0.5rem;
    border-top: 1px dashed var(--color-rule);
  }
  .bubble__src-pill {
    font-family: var(--font-mono);
    font-size: 0.68rem;
    padding: 0.18rem 0.45rem;
    background: color-mix(in oklch, var(--color-geo) 8%, var(--color-paper));
    border: 1px solid color-mix(in oklch, var(--color-geo) 20%, var(--color-rule));
    border-radius: 999px;
    color: oklch(0.42 0.13 155);
  }

  /* (Footer pod každou kartou byl odstraněn — verdict bubble nad kartou
   *  ho nahrazuje. Discipline tone tokeny zůstávají na .tpanel pro jiné
   *  případné použití, ale .tpanel__foot* CSS již není potřeba.)
   */
  .tpanel[data-tone="google"] { --tone-accent: oklch(0.5 0.18 256); }
  .tpanel[data-tone="aio"]    { --tone-accent: var(--color-accent); }
  .tpanel[data-tone="chat"]   { --tone-accent: oklch(0.5 0.13 155); }

  /* Callout pod trio — tato pasáž má uživatel **přečíst**.
   * Card s accent borderem, krouhový ! marker, větší tučný text.
   * Left-aligned, max-width 56rem pro čtecí komfort. */
  .trio__callout {
    max-width: 56rem;
    margin: clamp(2rem, 3.5vw, 2.75rem) 0 0;
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    padding: clamp(1.25rem, 2vw, 1.5rem) clamp(1.25rem, 2.5vw, 1.75rem);
    background: linear-gradient(
      135deg,
      color-mix(in oklch, var(--color-accent) 7%, var(--color-paper)) 0%,
      color-mix(in oklch, var(--color-aio) 5%, var(--color-paper)) 100%
    );
    border: 1px solid color-mix(in oklch, var(--color-accent) 30%, var(--color-rule-strong));
    border-left: 4px solid var(--color-accent);
    border-radius: 10px;
  }
  .trio__callout-mark {
    flex-shrink: 0;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    background: var(--color-accent);
    color: var(--color-paper);
    border-radius: 50%;
    font-family: var(--font-sans);
    font-size: 1.1rem;
    font-weight: 700;
    line-height: 1;
    margin-top: 0.1rem;
  }
  .trio__callout-text {
    margin: 0;
    font-family: var(--font-sans);
    font-size: clamp(1rem, 1.3vw, 1.125rem);
    line-height: 1.55;
    color: var(--color-ink);
  }
  .trio__callout-text :global(strong) {
    font-weight: 600;
    color: var(--color-ink);
    display: block;
    margin-bottom: 0.25rem;
  }
  .trio__callout-text :global(em) {
    font-style: italic;
    color: var(--color-accent);
    font-weight: 600;
  }
  .trio__callout-cont {
    color: var(--color-ink-muted);
    font-weight: 400;
    font-size: 0.95em;
  }

  /* ===== STAT BAR ===== */
  .stat-bar {
    border-top: 1px solid var(--color-rule-strong);
    border-bottom: 1px solid var(--color-rule-strong);
    background: var(--color-paper-deep);
  }
  .stat-bar__inner {
    max-width: var(--max-section);
    margin: 0 auto;
    padding: clamp(2rem, 4vw, 3.5rem) var(--space-section-x);
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 0;
  }
  .stat {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    padding: 1rem 1.5rem 1rem 1.25rem;
    border-left: 1px solid var(--color-rule);
  }
  .stat:first-child {
    border-left: none;
    padding-left: 0;
  }
  .stat__num {
    font-family: var(--font-sans);
    font-weight: 280;
    font-size: clamp(2.25rem, 4.5vw, 3.5rem);
    line-height: 1;
    letter-spacing: -0.05em;
    color: var(--color-ink);
    font-feature-settings: "tnum", "cv11";
    font-variant-numeric: tabular-nums;
  }
  .stat__num :global(strong) {
    font-weight: 600;
    color: var(--color-accent);
  }
  .stat__label {
    font-family: var(--font-sans);
    font-size: 0.875rem;
    color: var(--color-ink-muted);
    line-height: 1.4;
  }
  .stat__src {
    font-family: var(--font-sans);
    font-size: 0.78rem;
    font-weight: 400;
    color: var(--color-ink-faint);
    margin-top: 0.25rem;
    display: inline-flex;
    /* flex-start (ne center) — při zalomení textu na víc řádků by center
       vycentroval tečku na střed celého bloku a vypadala jako useknutá. */
    align-items: flex-start;
    gap: 0.4rem;
  }
  .stat__src::before {
    content: "";
    flex: none;
    width: 4px;
    height: 4px;
    /* opticky vystředit tečku na první řádek textu */
    margin-top: 0.5em;
    background: var(--color-ink-faint);
    border-radius: 50%;
  }

  /* ===== SECTION HEAD ===== */
  .section {
    max-width: var(--max-section);
    margin: 0 auto;
    padding: var(--space-section-y) var(--space-section-x);
  }
  /* Full-bleed paper-deep variant — pro vizuální oddělení sekcí.
   * Section sám zabírá celé okno (full-width background), ale vnitřní
   * obsah má STEJNOU šířku jako ostatní sekce v webu (header, hero, atd.):
   * 80rem mínus 2× horizontal padding token. Bez '+ var(--space-section-x)'
   * v calc() by obsah byl plných 80rem široký, což by ho udělalo o 2× token
   * širší než header content → karty by trčely za hranice kontejneru. */
  .section--deep {
    background: var(--color-paper-deep);
    border-top: 1px solid var(--color-rule);
    border-bottom: 1px solid var(--color-rule);
    max-width: none;
    width: 100%;
    margin: 0;
    padding-left: max(var(--space-section-x), calc(50vw - 40rem + var(--space-section-x)));
    padding-right: max(var(--space-section-x), calc(50vw - 40rem + var(--space-section-x)));
  }
  .section__head {
    margin-bottom: clamp(2.5rem, 4vw, 4rem);
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }
  .section__title {
    font-family: var(--font-sans);
    font-weight: 280;
    font-size: clamp(1.75rem, 4vw, 3rem);
    line-height: 1.05;
    letter-spacing: -0.04em;
    max-width: 22ch;
    color: var(--color-ink);
  }
  .section__title :global(strong) { font-weight: 600; }
  .section__title :global(.hl) {
    color: var(--color-accent);
    font-weight: 500;
  }
  .section__intro {
    font-family: var(--font-sans);
    font-weight: 350;
    font-size: clamp(1rem, 1.4vw, 1.1875rem);
    color: var(--color-ink-muted);
    max-width: 38rem;
    line-height: 1.55;
  }

  /* ===== QUARTET ===== */
  .quartet {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 1px;
    background: var(--color-rule);
    border: 1px solid var(--color-rule);
    border-radius: 8px;
    overflow: hidden;
  }
  .qcard {
    background: var(--color-paper);
    padding: clamp(1.75rem, 2.5vw, 2.25rem);
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    position: relative;
    transition: background-color 0.3s ease;
    min-height: 26rem;
    text-decoration: none;
    color: inherit;
  }
  .qcard:hover { background: var(--qcard-tint); }
  .qcard[data-d="seo"] {
    --qcard-color: var(--color-seo);
    --qcard-tint: var(--color-seo-tint);
  }
  .qcard[data-d="geo"] {
    --qcard-color: var(--color-geo);
    --qcard-tint: var(--color-geo-tint);
  }
  .qcard[data-d="aeo"] {
    --qcard-color: var(--color-aeo);
    --qcard-tint: var(--color-aeo-tint);
  }
  .qcard[data-d="aio"] {
    --qcard-color: var(--color-aio);
    --qcard-tint: var(--color-aio-tint);
  }
  .qcard__abbr {
    font-family: var(--font-sans);
    font-weight: 600;
    font-size: clamp(2.5rem, 4vw, 3.5rem);
    line-height: 0.95;
    letter-spacing: -0.05em;
    color: var(--qcard-color);
    margin: 0;
  }
  .qcard__name {
    font-family: var(--font-sans);
    font-weight: 500;
    font-size: 1.0625rem;
    line-height: 1.3;
    color: var(--color-ink);
    letter-spacing: -0.01em;
  }
  .qcard__desc {
    font-size: 0.9375rem;
    line-height: 1.55;
    color: var(--color-ink-muted);
  }
  .qcard__features {
    margin: auto 0 0;
    display: flex;
    flex-direction: column;
    gap: 0.625rem;
    border-top: 1px solid var(--color-rule);
    padding: 1.25rem 0 0;
    list-style: none;
    font-size: 0.875rem;
    color: var(--color-ink-muted);
    line-height: 1.45;
  }
  .qcard__features li {
    display: flex;
    align-items: flex-start;
    gap: 0.625rem;
  }
  .qcard__features li::before {
    content: "";
    flex: none;
    width: 6px;
    height: 6px;
    margin-top: 0.6em;
    border-radius: 50%;
    background: var(--qcard-color);
    opacity: 0.55;
  }
  .qcard__cta {
    margin-top: 1.25rem;
    font-family: var(--font-sans);
    font-size: 0.95rem;
    font-weight: 500;
    color: var(--qcard-color);
    display: flex;
    align-items: center;
    gap: 0.5rem;
    transition: gap 0.25s ease;
  }
  .qcard:hover .qcard__cta { gap: 0.875rem; }

  /* ===== ANATOMY (sekce 03 — ideální AI-friendly stránka) ===== */
  .anatomy-section {
    background: var(--color-paper-deep);
    border-top: 1px solid var(--color-rule);
    border-bottom: 1px solid var(--color-rule);
  }
  .anatomy-section__inner {
    max-width: var(--max-section);
    margin: 0 auto;
    padding: var(--space-section-y) var(--space-section-x);
  }
  .anatomy-section__head {
    margin-bottom: clamp(3rem, 5vw, 4rem);
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .anatomy-section__layout {
    display: grid;
    grid-template-columns: 1fr;
    gap: clamp(2.5rem, 4vw, 4rem);
    align-items: start;
  }
  @media (min-width: 1024px) {
    .anatomy-section__layout {
      grid-template-columns: minmax(0, 480px) 1fr;
      gap: clamp(2.5rem, 4vw, 4rem);
    }
  }

  .anatomy-svg {
    width: 100%;
    height: auto;
    max-width: 480px;
    margin: 0 auto;
    filter: drop-shadow(0 12px 32px oklch(0.22 0.012 260 / 0.06));
  }
  @keyframes anatomy-mark-in {
    from { opacity: 0; transform: scale(0.7); }
    to { opacity: 1; transform: scale(1); }
  }
  .anatomy-svg .anatomy-mark {
    transform-origin: center;
    transform-box: fill-box;
    animation: anatomy-mark-in 0.5s backwards cubic-bezier(0.2, 0.7, 0.2, 1);
  }
  .anatomy-svg .anatomy-mark:nth-of-type(1) { animation-delay: 0.2s; }
  .anatomy-svg .anatomy-mark:nth-of-type(2) { animation-delay: 0.32s; }
  .anatomy-svg .anatomy-mark:nth-of-type(3) { animation-delay: 0.44s; }
  .anatomy-svg .anatomy-mark:nth-of-type(4) { animation-delay: 0.56s; }
  .anatomy-svg .anatomy-mark:nth-of-type(5) { animation-delay: 0.68s; }
  .anatomy-svg .anatomy-mark:nth-of-type(6) { animation-delay: 0.80s; }

  .anatomy-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    counter-reset: anatomy;
  }
  .anatomy-item {
    display: grid;
    grid-template-columns: 2.5rem 1fr;
    gap: 1rem;
    padding: 1rem 1.25rem;
    background: var(--color-paper);
    border: 1px solid var(--color-rule);
    border-radius: 10px;
    transition: border-color 0.2s ease, transform 0.2s ease;
  }
  .anatomy-item:hover {
    border-color: var(--color-accent);
    transform: translateX(2px);
  }
  .anatomy-item__num {
    width: 2rem;
    height: 2rem;
    border-radius: 50%;
    background: var(--color-accent);
    color: var(--color-paper);
    font-family: var(--font-sans);
    font-size: 0.85rem;
    font-weight: 600;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    align-self: flex-start;
  }
  .anatomy-item__title {
    font-family: var(--font-sans);
    font-weight: 600;
    font-size: 1rem;
    line-height: 1.3;
    letter-spacing: -0.012em;
    margin: 0 0 0.25rem;
    color: var(--color-ink);
  }
  .anatomy-item__desc {
    font-family: var(--font-sans);
    font-size: 0.9rem;
    line-height: 1.5;
    color: var(--color-ink-muted);
    margin: 0;
  }
  .anatomy-item__desc :global(code) {
    font-family: var(--font-mono);
    font-size: 0.85em;
    background: var(--color-paper-deep);
    padding: 0.1em 0.35em;
    border-radius: 4px;
    color: var(--color-ink);
  }

  /* ===== PILLAR PROMO ===== */
  .pillar-promo {
    max-width: var(--max-section);
    margin: 0 auto;
    padding: var(--space-section-y) var(--space-section-x);
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    align-items: stretch;
  }
  @media (min-width: 900px) {
    .pillar-promo { grid-template-columns: 1.3fr 1fr; gap: 5rem; }
  }
  .pillar-promo__main {
    border-top: 1px solid var(--color-ink);
    padding-top: 2rem;
    display: flex;
    flex-direction: column;
    gap: 1.75rem;
  }
  .pillar-promo__title {
    font-family: var(--font-sans);
    font-weight: 280;
    font-size: clamp(2rem, 4.5vw, 3.5rem);
    line-height: 1;
    letter-spacing: -0.045em;
    color: var(--color-ink);
    margin: 0;
  }
  .pillar-promo__title :global(strong) { font-weight: 600; }
  .pillar-promo__title :global(.hl) {
    color: var(--color-accent);
    font-weight: 500;
  }
  .pillar-promo__lead {
    font-family: var(--font-sans);
    font-weight: 350;
    font-size: 1.1875rem;
    line-height: 1.5;
    color: var(--color-ink-muted);
    max-width: 36rem;
  }
  .pillar-promo__meta {
    font-family: var(--font-sans);
    font-size: 0.9rem;
    color: var(--color-ink-muted);
    display: flex;
    flex-wrap: wrap;
    gap: 1.5rem;
    padding-top: 0.5rem;
  }
  .pillar-promo__meta span {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }
  .pillar-promo__meta span::before {
    content: "";
    width: 6px;
    height: 6px;
    background: var(--color-accent);
    border-radius: 50%;
  }

  .btn {
    align-self: flex-start;
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    font-family: var(--font-sans);
    font-weight: 500;
    font-size: 1rem;
    padding: 0.95rem 1.625rem;
    background: var(--color-ink);
    color: var(--color-paper);
    border-radius: 999px;
    border: none;
    cursor: pointer;
    transition: all 0.2s ease;
    letter-spacing: -0.005em;
    text-decoration: none;
  }
  .btn:hover {
    background: var(--color-accent);
    transform: translateX(2px);
  }
  .btn__arrow { transition: transform 0.25s ease; }
  .btn:hover .btn__arrow { transform: translateX(3px); }

  .pillar-promo__excerpt {
    background: var(--color-paper-deep);
    padding: clamp(1.75rem, 2.5vw, 2.5rem);
    border-radius: 8px;
    border: 1px solid var(--color-rule);
    border-left: 3px solid var(--color-accent);
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }
  .pillar-promo__excerpt-mark {
    font-family: var(--font-sans);
    font-size: 0.8rem;
    font-weight: 500;
    color: var(--color-accent);
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }
  .pillar-promo__excerpt-text {
    font-family: var(--font-sans);
    font-weight: 380;
    font-size: 1.1875rem;
    line-height: 1.45;
    letter-spacing: -0.012em;
    color: var(--color-ink);
  }
  .pillar-promo__excerpt-text :global(strong) { font-weight: 600; }

  /* ===== SECTION GRID (vybrané články) ===== */
  .section-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1rem;
  }
  .section-more {
    display: flex;
    justify-content: center;
    margin-top: 2rem;
  }
  .section-more__btn {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    font-family: var(--font-sans);
    font-size: 0.9375rem;
    font-weight: 600;
    color: var(--color-ink);
    background: var(--color-paper);
    border: 1px solid var(--color-rule-strong);
    border-radius: 999px;
    padding: 0.75rem 1.5rem;
    text-decoration: none;
    transition: transform 0.15s ease, box-shadow 0.15s ease, border-color 0.2s ease, background 0.2s ease;
  }
  .section-more__btn svg {
    transition: transform 0.2s ease;
  }
  .section-more__btn:hover {
    transform: translateY(-1px);
    border-color: var(--color-ink);
    box-shadow: 0 6px 16px -8px oklch(0 0 0 / 0.18);
  }
  .section-more__btn:hover svg {
    transform: translateX(3px);
  }
  .section-more__btn:focus-visible {
    outline: 2px solid var(--color-ink);
    outline-offset: 3px;
  }
  .scard {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    padding: 1.75rem;
    background: var(--color-paper);
    border: 1px solid var(--color-rule);
    border-radius: 8px;
    transition: all 0.25s ease;
    text-decoration: none;
    color: inherit;
    position: relative;
    overflow: hidden;
  }
  .scard::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 3px;
    height: 100%;
    background: var(--scard-color, var(--color-ink-faint));
    transform: scaleY(0.3);
    transform-origin: top;
    transition: transform 0.35s cubic-bezier(0.5, 0, 0, 1);
  }
  .scard:hover {
    background: var(--color-paper-deep);
    border-color: var(--scard-color, var(--color-ink));
  }
  .scard:hover::before { transform: scaleY(1); }
  .scard[data-d="practice"] { --scard-color: var(--color-accent); }
  .scard[data-d="matrix"] { --scard-color: oklch(0.4 0.06 200); }
  .scard[data-d="defense"] { --scard-color: var(--color-signal); }

  .scard__header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 0.25rem;
  }
  .scard__tag {
    font-family: var(--font-sans);
    font-size: 0.78rem;
    font-weight: 500;
    color: var(--scard-color);
    background: oklch(from var(--scard-color) l c h / 0.08);
    padding: 0.25rem 0.625rem;
    border-radius: 999px;
    letter-spacing: 0.02em;
  }
  .scard__time {
    font-family: var(--font-sans);
    font-size: 0.78rem;
    color: var(--color-ink-faint);
  }
  .scard__title {
    font-family: var(--font-sans);
    font-weight: 500;
    font-size: 1.1875rem;
    line-height: 1.25;
    letter-spacing: -0.018em;
    color: var(--color-ink);
    margin: 0;
  }
  .scard__desc {
    font-size: 0.9rem;
    line-height: 1.55;
    color: var(--color-ink-muted);
  }
  .scard__meta {
    margin-top: auto;
    padding-top: 0.875rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-top: 1px solid var(--color-rule);
    font-family: var(--font-sans);
    font-size: 0.875rem;
    color: var(--color-ink-faint);
  }
  .scard__cta {
    color: var(--scard-color);
    transition: gap 0.25s ease;
    display: flex;
    gap: 0.4rem;
    font-weight: 500;
  }
  .scard:hover .scard__cta { gap: 0.7rem; }

  /* ===== NABÍDKA — 3 produkty (sekce 06) ===== */
  .offers {
    max-width: var(--max-section);
    margin: 0 auto;
    padding: clamp(3rem, 6vw, 5rem) var(--space-section-x);
  }
  .offers__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: clamp(1.25rem, 2.5vw, 1.75rem);
  }
  @media (min-width: 768px) {
    .offers__grid { grid-template-columns: repeat(3, 1fr); }
  }
  .offer {
    --offer-accent: oklch(0.55 0.18 240);
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 0.875rem;
    background: var(--color-paper);
    border: 1px solid var(--color-rule);
    border-radius: 14px;
    padding: 1.75rem 1.625rem 1.5rem;
    text-decoration: none;
    color: inherit;
    transition: border-color 0.2s ease, transform 0.2s ease, box-shadow 0.2s ease;
  }
  .offer:hover {
    border-color: var(--offer-accent);
    transform: translateY(-3px);
    box-shadow: 0 14px 30px -16px oklch(from var(--offer-accent) l c h / 0.35);
  }
  .offer--free { --offer-accent: oklch(0.45 0.14 145); }
  .offer--pack { --offer-accent: oklch(0.5 0.25 305); }
  .offer--audit {
    --offer-accent: oklch(0.5 0.27 335);
    border-width: 2px;
    border-color: oklch(from var(--offer-accent) l c h / 0.6);
    background: linear-gradient(180deg, oklch(from var(--offer-accent) l c h / 0.04) 0%, var(--color-paper) 50%);
  }
  .offer__featured-pill {
    position: absolute;
    top: -0.6rem;
    right: 1.5rem;
    font-family: var(--font-mono);
    font-size: 0.65rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #fff;
    background: var(--offer-accent);
    padding: 0.3rem 0.75rem;
    border-radius: 999px;
    box-shadow: 0 4px 10px -3px oklch(from var(--offer-accent) l c h / 0.5);
  }
  .offer__head {
    display: flex;
    flex-direction: column;
    gap: 0.625rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--color-rule);
  }
  .offer__tag {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--offer-accent);
  }
  .offer__price {
    display: flex;
    flex-direction: column;
    gap: 0.125rem;
  }
  .offer__price :global(strong) {
    font-family: var(--font-sans);
    font-size: 1.625rem;
    font-weight: 700;
    line-height: 1.1;
    letter-spacing: -0.02em;
    color: var(--color-ink);
  }
  .offer__price :global(small) {
    font-family: var(--font-mono);
    font-size: 0.72rem;
    font-weight: 500;
    letter-spacing: 0.04em;
    color: var(--color-ink-faint);
  }
  .offer__price-vat {
    margin-top: 0.125rem;
    color: var(--color-ink-muted);
  }
  .offer__price--free :global(strong) { color: oklch(0.45 0.14 145); }
  .offer__price--pack :global(strong) { color: oklch(0.45 0.22 305); }
  .offer__price--audit :global(strong) { color: oklch(0.5 0.25 335); }
  .offer__title {
    font-family: var(--font-sans);
    font-weight: 600;
    font-size: 1.1875rem;
    line-height: 1.3;
    letter-spacing: -0.018em;
    color: var(--color-ink);
    margin: 0;
  }
  .offer__desc {
    font-size: 0.9rem;
    line-height: 1.55;
    color: var(--color-ink-muted);
    margin: 0;
  }
  .offer__bullets {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
    flex: 1;
  }
  .offer__bullets li {
    font-size: 0.85rem;
    line-height: 1.5;
    color: var(--color-ink-muted);
    padding-left: 1.125rem;
    position: relative;
  }
  .offer__bullets li::before {
    content: "✓";
    position: absolute;
    left: 0;
    top: 0;
    color: var(--offer-accent);
    font-weight: 700;
    font-size: 0.8rem;
  }
  .offer__cta {
    margin-top: 0.5rem;
    padding-top: 0.875rem;
    border-top: 1px solid var(--color-rule);
    font-family: var(--font-sans);
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--offer-accent);
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    transition: gap 0.2s ease;
  }
  .offer:hover .offer__cta { gap: 0.7rem; }

  /* ===== Mobil (telefony ≤600px) — utáhnout vzdušné mezery =====
     Hlavní rytmus sekcí řeší globálně token --space-section-y (global.css).
     Tady jen bespoke metriky homepage, které token nepoužívají: hero má
     vh-based padding (89px nahoře na mobilu), section-head vlastní margin.
     Desktop (>600px) beze změny — base hodnoty zůstávají. */
  @media (max-width: 600px) {
    .hero {
      padding-top: clamp(2rem, 5vh, 3.5rem);
      padding-bottom: clamp(1.75rem, 4vh, 2.75rem);
    }
    .section__head {
      margin-bottom: clamp(1.5rem, 6vw, 2.5rem);
    }
    /* Offers má vlastní inline clamp (48px) — srovnat do rytmu token sekcí. */
    .offers {
      padding-top: clamp(2.25rem, 9vw, 3rem);
      padding-bottom: clamp(2.25rem, 9vw, 3rem);
    }
    /* Stat-bar: border-left dividery + first-child reset jsou navržené pro
       1řádkový desktop. Na mobilní mřížce dělaly stray border-left + odsazení
       u levého sloupce (např. ~60 % se nezarovnalo). Reset → vše flush-left,
       separace přes grid gap. */
    .stat-bar__inner {
      gap: 1.5rem 1.25rem;
    }
    .stat,
    .stat:first-child {
      border-left: none;
      padding: 0;
    }
  }
</style>

<script>
  // Pill popover toggle — click to open, click again or X to close, Esc to close,
  // click outside to close. Only one panel visible at a time.
  (() => {
    const triggers = document.querySelectorAll<HTMLButtonElement>("[data-pill-trigger]");
    const panels = document.querySelectorAll<HTMLElement>("[data-pill-panel]");
    const container = document.querySelector<HTMLElement>("[data-pill-panels]");

    if (!triggers.length || !panels.length || !container) return;

    const closeAll = () => {
      triggers.forEach((t) => t.setAttribute("aria-expanded", "false"));
      panels.forEach((p) => p.setAttribute("hidden", ""));
    };

    const open = (key: string) => {
      panels.forEach((p) => {
        if (p.dataset.pillPanel === key) p.removeAttribute("hidden");
        else p.setAttribute("hidden", "");
      });
      triggers.forEach((t) => {
        t.setAttribute(
          "aria-expanded",
          t.dataset.pillTrigger === key ? "true" : "false",
        );
      });
    };

    triggers.forEach((trigger) => {
      trigger.addEventListener("click", (e) => {
        e.stopPropagation();
        const key = trigger.dataset.pillTrigger;
        if (!key) return;
        const isOpen = trigger.getAttribute("aria-expanded") === "true";
        if (isOpen) closeAll();
        else open(key);
      });
    });

    // Close button inside panels
    document.querySelectorAll<HTMLButtonElement>("[data-pill-close]").forEach((btn) => {
      btn.addEventListener("click", (e) => {
        e.stopPropagation();
        closeAll();
      });
    });

    // Click outside both pills and panels closes
    document.addEventListener("click", (e) => {
      const target = e.target as Node;
      const insidePanels = container.contains(target);
      const insideTrigger = Array.from(triggers).some((t) => t.contains(target));
      if (!insidePanels && !insideTrigger) closeAll();
    });

    // Escape closes
    document.addEventListener("keydown", (e) => {
      if (e.key === "Escape") closeAll();
    });
  })();
</script>
